# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

group :development, :test do
  # Optional at runtime, required to exercise the table tagging patches.
  gem 'prawn-table', '~> 0.2'

  gem 'rake', '~> 13.0'
  gem 'rspec', '~> 3.0'
  gem 'rubocop', '~> 1.60'
end
